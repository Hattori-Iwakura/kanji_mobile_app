import 'package:flutter/material.dart';

/// Widget for drawing canvas - placeholder for future custom implementation
class DrawingCanvas extends StatelessWidget {
  const DrawingCanvas({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: const Center(child: Text('Drawing Canvas')),
    );
  }
}
